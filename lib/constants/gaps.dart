import 'package:flutter/material.dart';

class Gaps {
  static SizedBox h(double size) {
    return SizedBox(
      width: size,
    );
  }

  static SizedBox v(double size) {
    return SizedBox(
      height: size,
    );
  }
}
